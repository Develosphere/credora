# =============================================================================
# Credora CFO Agent - Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your values
# All keys marked [REQUIRED] must be set for the system to work

# =============================================================================
# LLM CONFIGURATION [REQUIRED]
# =============================================================================
# Get your API key from: https://openrouter.ai/
# 1. Sign up or log in at OpenRouter
# 2. Go to API Keys section
# 3. Create a new key and paste it below
OPENROUTER_API_KEY=your_openrouter_api_key_here

# Optional: Add a second API key for rotation when rate limited
# The system will automatically use this when the first key hits rate limits
OPENROUTER_API_KEY_2=your_second_openrouter_api_key_here

# =============================================================================
# OAUTH SERVER CONFIGURATION
# =============================================================================
# The OAuth callback server runs locally to handle platform authorization
# Default: http://localhost:8000
OAUTH_SERVER_HOST=http://localhost
OAUTH_SERVER_PORT=8000

# =============================================================================
# META (FACEBOOK/INSTAGRAM) ADS [REQUIRED for Meta integration]
# =============================================================================
# Get credentials from: https://developers.facebook.com/
# 1. Create a new app at developers.facebook.com
# 2. Add "Marketing API" product to your app
# 3. Go to Settings > Basic to get App ID and App Secret
# 4. Add OAuth redirect URI: http://localhost:8000/oauth/callback/meta

META_CLIENT_ID=33652467304351534
META_CLIENT_SECRET=b4ffb8d6a4d7f007b4e11a0f2e7f2283
META_REDIRECT_URI=http://localhost:8000/oauth/callback/meta

# =============================================================================
# GOOGLE ADS [REQUIRED for Google integration]
# =============================================================================
# Get credentials from: https://console.cloud.google.com/
# 1. Create a new project in Google Cloud Console
# 2. Enable "Google Ads API"
# 3. Go to APIs & Services > Credentials
# 4. Create OAuth 2.0 Client ID (Web application)
# 5. Add authorized redirect URI: http://localhost:8000/oauth/callback/google
# 6. Get Developer Token from Google Ads API Center

GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret
GOOGLE_REDIRECT_URI=http://localhost:8000/oauth/callback/google
GOOGLE_DEVELOPER_TOKEN=your_google_developer_token

# =============================================================================
# SHOPIFY [REQUIRED for Shopify integration]
# =============================================================================
# Get credentials from: https://partners.shopify.com/
# 1. Create a Shopify Partner account
# 2. Create a new app in your Partner Dashboard
# 3. Go to App Setup to get API key and API secret
# 4. Add redirect URL: http://localhost:8000/oauth/callback/shopify
# 5. Request the following scopes:
#    - read_orders
#    - read_products
#    - read_customers
#    - read_analytics

SHOPIFY_CLIENT_ID=your_shopify_api_key
SHOPIFY_CLIENT_SECRET=your_shopify_api_secret
SHOPIFY_REDIRECT_URI=http://localhost:8000/oauth/callback/shopify

# =============================================================================
# NOTES
# =============================================================================
# 
# 1. You only need to configure the platforms you want to use.
#    - For Shopify only: Set OPENROUTER_API_KEY and SHOPIFY_* variables
#    - For Meta only: Set OPENROUTER_API_KEY and META_* variables
#    - For Google only: Set OPENROUTER_API_KEY and GOOGLE_* variables
#
# 2. For production deployment:
#    - Change OAUTH_SERVER_HOST to your public domain
#    - Update all redirect URIs to use HTTPS
#    - Use proper secret management (not .env files)
#
# 3. OAuth Flow:
#    - Start the OAuth server: uv run python -m credora.oauth_server
#    - Start the CLI: uv run main.py
#    - When connecting a platform, visit the provided URL
#    - After authorization, you'll be redirected back automat